CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
# set(CMAKE_CXX_STANDARD 17)

PROJECT(QEDIS)

SUBDIRS(QBase)
SUBDIRS(QedisCore)
SUBDIRS(QedisSvr)

# for courses and test
SUBDIRS(courses/lab2/code1)
SUBDIRS(courses/lab2/code2)


# Do not use cluster
SET(QEDIS_CLUSTER 0)
SET(USE_ZOOKEEPER 0)

ADD_DEFINITIONS(-DQEDIS_CLUSTER=${QEDIS_CLUSTER})
ADD_DEFINITIONS(-DUSE_ZOOKEEPER=${USE_ZOOKEEPER})
